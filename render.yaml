services:
  - type: web
    name: BankNiftyApp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run src/Home_Dashboard.py --server.port 8501 --server.address 0.0.0.0
